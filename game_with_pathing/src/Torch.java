import processing.core.PImage;

import java.util.List;

public class Torch extends AnimatedEntity
{
    public Torch(String id, Point pos, List<PImage> images, int animationPeriod)
    {
        super(id, pos, images, animationPeriod);
    }
}
